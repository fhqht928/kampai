# ============================================
# Kampai 환경 변수 설정 예시
# 이 파일을 .env로 복사 후 실제 값을 입력하세요
# ============================================

# 서버 환경 (development / production)
KAMPAI_ENV=development

# Replicate API (AI 이미지 생성)
# https://replicate.com/account/api-tokens 에서 발급
REPLICATE_API_TOKEN=r8_여기에_실제_토큰_입력

# TossPayments (결제 시스템)
# https://developers.tosspayments.com 에서 발급
# 테스트 키 (개발용)
TOSS_SECRET_KEY=test_sk_zXLkKEypNArWmo50nX3lmeaxYG5R
TOSS_CLIENT_KEY=test_ck_D5GePWvyJnrK0W0k6q8gLzN97Eoq

# JWT 시크릿 (인증용) - 랜덤 문자열 사용
# python -c "import secrets; print(secrets.token_hex(32))" 로 생성 가능
JWT_SECRET=change_this_to_random_secret_key_in_production

# 데이터베이스 경로
DB_PATH=D:/AI_Work/kampai.db

# ComfyUI 설정 (개발용)
COMFYUI_HOST=localhost
COMFYUI_PORT=8188
