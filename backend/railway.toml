[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn server:app --bind 0.0.0.0:5000 --workers 2 --timeout 120"
healthcheckPath = "/api/health"
healthcheckTimeout = 100

[[services]]
name = "web"
internal_port = 5000
